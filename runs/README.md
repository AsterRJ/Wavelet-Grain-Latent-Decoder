# Local Runs Directory

This directory is tracked only as a placeholder. Checkpoints, latent archives,
metric archives, priors, generated images, and metrics are private/generated and
ignored by Git.

Current best local artifact set:

```text
runs/raw_learned_wavelet_codec/
runs/raw_learned_wavelet_metric/
runs/raw_learned_wavelet_prior_wide/
runs/raw_learned_wavelet_auto/
```

Use `README.md` for the commands that recreate these artifacts.
